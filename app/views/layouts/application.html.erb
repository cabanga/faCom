<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "FaCom" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= javascript_include_tag "application" %>
  </head>

  <body class=" <%= @color_bg %>">
    <% if usuario_signed_in? %>
      <nav class="navbar navbar-default navbar-static-top">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="<%=root_url %>">FaCom - <%= current_usuario.empresa.nome %> </a>
          </div>
          <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-right">
              <li><%= link_to 'Sair <span class="fa fa-sign-out "></span>'.html_safe, destroy_usuario_session_url, method: :delete %></li>
            </ul>
          </div>
        </div>
      </nav>


    <% end %>

      <div class="container">
        <div class="row">
          <%= yield %>
          <%= bootstrap_flash %>
        </div><!--/row-->
      </div> <!-- /container -->


      <div>

  </body>
</html>
